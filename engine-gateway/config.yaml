server:
  port: 8090

upstream:
  base_url: "http://127.0.0.1:8081"
  connect_timeout: 2s
  read_timeout: 60s
  max_body_bytes: 57671680

health:
  probe_interval: 10s
  failure_threshold: 3

static:
  dir: "./static"

logging:
  level: "info"
  format: "text"