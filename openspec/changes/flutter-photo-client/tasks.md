# tasks — Flutter 照片客户端（lifeCloud_app）

> Phase A（演示数据）不依赖后端变更，可并行实施；Phase B 依赖 `photo-semantic-search` 已实施。

## 1. 脚手架与基建（Phase A）

- [x] 1.1 `flutter create` 生成 `F:\workspace\lifeCloud_app`（Android+Windows），清理模板页；验证：`flutter run -d windows` 出空白壳 App
- [x] 1.2 添加依赖（dio / provider / image_picker / cached_network_image / shared_preferences / flutter_staggered_grid_view；Windows 选图按 design D1 评估 file_picker）；验证：`flutter pub get` 零冲突、`flutter analyze` 无告警
- [x] 1.3 core 层：models（对齐后端 DTO + 演示样例模型）、theme（D7 视觉规范：色/形/距/字）、`PhotoRepository` 抽象接口（分页列表/详情/搜索/上传任务/统计）；验证：单测覆盖 model 序列化与 repository 接口契约签名
- [x] 1.4 主导航壳：底部五槽（首页/相册/中央凸起＋/搜索/我的）IndexedStack 保状态 + named routes 路由表（design D2）；验证：四 Tab 切换状态保持（spec 场景）与中央＋进入上传流

## 2. 演示数据层（Phase A）

- [x] 2.1 `CustomPainter` 确定性占位图生成（种子→渐变+几何图形，同 id 同图）；验证：同 id 两次渲染像素一致的单测
- [x] 2.2 `MockPhotoRepository`：24 张内置样例（标注语义对齐"红色的小花/红塔与白云"等、覆盖 mocked/未拆分/高低分三类）、分页列表、统计计数；验证：单测断言样例分布与分页无重复
- [x] 2.3 演示搜索（查询词×描述重合度确定性打分排序）与演示上传任务（200ms/张递增、注入 1 失败样例、明示未真实入库）；验证：查"红色的小花"首结果为对应样例的单测；演示任务终态含失败明细

## 3. 首页（Phase A）

- [x] 3.1 顶部胶囊搜索栏（占位"搜索照片：红色的小花…"，点击推入搜索页并聚焦）+ 2×4 功能宫格（全部照片/最近上传/批量导入/模拟识别/未拆分/演示模式…）；验证：宫格带参跳转相册筛选态正确（spec 场景）、无死入口
- [x] 3.2 双列瀑布流卡片区（flutter_staggered_grid_view：缩略图/主题/描述/徽标/异高卡片）；验证：卡片三形态（mocked/未拆分/占位）视觉验收与滚动流畅

## 4. 相册页（Phase A）

- [x] 4.1 网格（三列）/瀑布流（双列）切换 + 偏好持久化 + 筛选 chips（全部/已标注/模拟识别/未拆分）；验证：切换重启后保持（spec 场景）、筛选结果与条件一致
- [x] 4.2 下拉刷新 + 滚动分页（追加无重复）；验证：样例数据分页边界用例（spec 场景）

## 5. 搜索页（Phase A）

- [x] 5.1 搜索输入/提交 + 历史 chips（本地持久化、去重、可清空）+ 推荐词；验证：重进搜索页历史可见可点（spec 场景）
- [x] 5.2 结果瀑布流：命中卡片（缩略图/描述/分数胶囊可视化 D7/mocked 徽标）+ loading/空态/错误态三态 + 「加深搜索」；验证：演示数据排序命中（spec 场景）与三态视觉验收

## 6. 大图预览（Phase A）

- [x] 6.1 Hero 共享元素进入全屏 + PageView 左右切换相邻 + 底部可展开标注面板（主题/描述/标签/分数）+ 顶部渐变返回；验证：Hero 动画连续、滑动切换面板同步（spec 场景）

## 7. 上传页（Phase A，演示任务）

- [x] 7.1 相册多选 + 已选横向管理（移除）+ 共享描述输入 + 提交进入进度页（全屏 modal 流）；验证：选 3 张提交流程走通、可返回壳
- [x] 7.2 进度页（环形进度+计数）绑定演示任务 + 失败明细 + 完成态"演示：未真实入库"明示；验证：失败样例明细可见（spec 场景）

## 8. 我的页与演示开关（Phase A）

- [x] 8.1 统计卡（总数/已标注/模拟识别/未拆分）+ 设置列表（后端地址/自检/演示模式开关/关于）；验证：计数与数据源一致（spec 场景）
- [x] 8.2 演示模式开关：切换即时刷新各页 + 横幅显隐（spec 场景）；验证：开关切换后首页/相册/搜索数据源即时更换、横幅联动
- [x] 8.3 设置子页（右侧推入）：地址编辑保存即生效 + 自检按钮；验证：地址校验与不可达提示（spec 场景）
- [x] 8.4 Phase A 端到端验收：Windows 全流程（浏览→搜索→预览→演示上传→开关切换）录屏/截图记录于验证文档；`flutter analyze` 清零 + `flutter test` 全绿

## 9. 真实 API 接入（Phase B，依赖后端变更实施完成）

- [ ] 9.1 `ApiPhotoRepository`：dio 实现全部 repository 方法（ApiResponse 解包、错误映射为三态）；验证：对运行中后端的契约单测（列表/搜索/上传任务）
- [ ] 9.2 真实上传：客户端分批编排（D5：切批/串行/轮询/合成进度/失败汇总）；验证：30 张真机/桌面导入进度连续、超上限自动分批（spec 场景）
- [ ] 9.3 真实搜索与画廊：关闭演示模式后全链路（caption 上传→模糊查询命中→预览）；验证：后端演示语义样例端到端命中（spec 场景）
- [ ] 9.4 Android 真机验收（局域网地址）：全流程 + 30 张批量；记录于验证文档；`flutter analyze`/`flutter test` 复验清零全绿
