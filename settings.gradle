/*
 * ============================================================
 * settings.gradle — 多模块工程的"户口本"
 * ------------------------------------------------------------
 * 【教学注释】Gradle 多模块工程从这里开始：
 *   1. rootProject.name 定义整个工程的根名称
 *   2. include 声明子模块（每个模块是一个独立的可构建单元）
 *   3. pluginManagement 管理插件从哪里下载（必须在文件最前面）
 *
 * 本工程只有一个 Gradle 模块：
 *   - engine-server  :8081  核心 DDD 业务（文档向量化/检索/QA）
 *
 * 注意：以下资产不进 Gradle sourceSet，由各自语言的原生工具构建，Gradle 负责
 * "编排"并随 engine-server 的 jar 一起提供（单 JAR 合体，见 docs/reqforge-single-jar-spec.md）：
 *   - engine-server/frontend/  npm 工程（React 前端，任务链挂在本模块 build.gradle，
 *                              产物入 jar 的 classpath:/static/）
 *   - engine-server/python/    Python 工程（向量化引擎，脚本入 jar 的 classpath:/python/）
 * 一个 Gradle 模块一棵完整资产树：Java + 前端 + Python 随同一个 jar 部署。
 * ============================================================
 */

pluginManagement {
    repositories {
        // 插件优先从国内镜像下载（Windows 开发机网络环境优化）
        maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
        maven { url 'https://maven.aliyun.com/repository/spring' }
        gradlePluginPortal()
        mavenCentral()
    }
    // 统一钉住插件版本：所有子模块用的 Spring Boot 插件版本完全一致
    plugins {
        id 'org.springframework.boot' version '2.6.14'
        id 'io.spring.dependency-management' version '1.0.15.RELEASE'
    }
}

rootProject.name = 'engine'

// 可部署的 Spring Boot 业务模块（网关已是 Go 工程，不在此列）
include 'engine-server'
